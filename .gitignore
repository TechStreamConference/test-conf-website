**/.env
.direnv/
**/.idea/
.pre-commit-config.yaml
# Local log spool files written by native backend/frontend processes.
# These are ephemeral transport files — Loki is the retained log store.
.logs/

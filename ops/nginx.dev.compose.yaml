services:
  nginx:
    image: nginx:1.31.2-alpine
    container_name: nginx-${ENVIRONMENT}
    volumes:
      - ./nginx/${ENVIRONMENT}/default.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
    network_mode: host
